from django.apps import AppConfig


class WeeklyMediaConfig(AppConfig):
    """Configuration for the Weekly Media app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "src.apps.weekly_media"
    label = "weekly_media"
